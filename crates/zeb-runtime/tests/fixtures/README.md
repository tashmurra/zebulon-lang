# Save-format fixtures

These binary fixtures contain independently authored logical-save version 1 data: an empty store and a small world. Runtime tests compare complete encoded bytes, decode the fixtures, and check restored properties, text and relations. They also reject truncation, incompatible versions, malformed identities and invalid graphs.

They are test inputs, not executable files or captured user sessions.
